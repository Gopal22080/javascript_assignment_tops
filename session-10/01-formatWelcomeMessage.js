// Q1/Q2 - Format welcome message using template literals
const formatWelcomeMessage = (userName, followers) => {
  return `Hello ${userName}, you have ${followers} followers!`;
};

console.log(formatWelcomeMessage("Gopal", 1200));
